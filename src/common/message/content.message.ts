export enum Content {
  // Global validate
  SUCCESSFULLY = 'successfully',
  FAILED = 'failed',

  // Optional validate
  REQUIRED = 'required',
  NOT_FOUND = 'not_found',
  INVALID = 'invalid',
  UNAUTHORIZATION = 'unauthorize',
  NOT_NULL = 'not_null',
}
